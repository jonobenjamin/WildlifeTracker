/**
 * PWA install button disabled — users install via the browser UI
 * (Chrome: Install / Add to Home screen). Kept as a no-op so old caches
 * that still request this file do not error.
 */
(function () {
  // Intentionally empty.
})();
